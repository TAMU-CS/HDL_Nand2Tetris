//Kevin Tang

/**
 * Negate a 16-bit number, be aware that first bit is the sign-bit. 
 * 2’s complement of the input 
 */

CHIP Negation {
    IN in[16];
    OUT out[16];

    PARTS:
	// Put you code here:
	Not16(in = in, out = o);
	Inc16(in = o, out = out);
}